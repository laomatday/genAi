label,fn){const b=document.createElement('button');b.className='uiBtn';b.textContent=label;b.onclick=()=>{fn();updateHud();persist();renderInventory()};$('npcActions').appendChild(b)}
function buy(cost,fn){if(save.coins<cost){toast('Không đủ coin');return}save.coins-=cost;fn();persist();toast('Đã mua')}
function updateHud(){$('hpFill').style.width=clamp(P.hp/P.max*100,0,100)+'%';$('floorPill').textContent='TẦNG '+save.floor+' · '+room.spec.name;$('coinPill').textContent=save.coins.toLocaleString('vi-VN');$('keyPill').textContent=save.keys;$('levelPill').textContent='LV '+save.level;const boss=enemies.find(e=>['slimeking','gobchief','cavequeen','abyss','centiqueen','dragonalpha','golemboss','halfdragon','corrupt','dracula','treant'].includes(e.type));if(boss){$('bossHud').style.display='block';$('bossName').textContent=bossLabel(boss);$('bossFill').style.width=clamp(boss.hp/boss.max*100,0,100)+'%'}else $('bossHud').style.display='none'}
function bossLabel(e){return{slimeking:'SLIME KING',gobchief:'GOBLIN CHIEF',cavequeen:'CAVE MATRIARCH',abyss:'ABYSS WARDEN',centiqueen:'CENTIPEDE QUEEN',dragonalpha:'DRAGON ALPHA',golemboss:'ELEMENTAL GOLEM',halfdragon:'BÁN LONG',corrupt:'TINH LINH THA HÓA',dracula:'DRACULA',treant:'ANCIENT TREANT'}[e.type]||e.type.toUpperCase()}
function drawBackground(){const img=(save.floor>=40||room.spec.name.includes('Goblin'))?ASSETS.goblin:ASSETS.ruins;const off=(cam*.12)%W;for(let i=-1;i<3;i++)X.drawImage(img,i*W-off,0,W,465);X.fillStyle='#061116';X.fillRect(0,GROUND,W,H-GROUND);X.fillStyle='#244f55';for(let x=-(cam%64);x<W;x+=64)X.fillRect(x,GROUND,52,8)}
function drawPlatforms(){X.fillStyle='#17383d';for(const p of room.platforms){const x=p.x-cam;if(x>-p.w&&x<W){X.fillRect(x,p.y,p.w,p.h);X.fillStyle='#29595c';X.fillRect(x,p.y,p.w,4);X.fillStyle='#17383d'}}}
function drawSigns(){X.fillStyle='#9a7b4d';const sign=(wx,text,dir)=>{const x=wx-cam;if(x<-120||x>W+120)return;X.fillRect(x,GROUND-76,8,76);X.fillRect(x-36,GROUND-86,80,28);X.fillStyle='#e5d7ad';X.font='700 12px monospace';X.textAlign='center';X.fillText((dir<0?'← ':'')+text+(dir>0?' →':''),x+4,GROUND-68);X.fillStyle='#9a7b4d'};sign(220,'KHÁM PHÁ',1);sign(WORLD-150,save.floor===100?'TẦNG CUỐI':'TẦNG '+Math.min(100,save.floor+1),1);if(!room.cleared){const gx=room.gateX-cam;X.fillStyle='#6b2729';X.fillRect(gx,160,16,GROUND-160);X.fillStyle='#ff765d';for(let y=170;y<GROUND;y+=32)X.fillRect(gx-5,y,26,4)}}
function drawNPC(){const n=room.npc;if(!n)return;const x=n.x-cam;X.save();X.fillStyle=n.kind==='shop'?'#d7b85b':n.kind==='healer'?'#dfe8d8':n.kind==='blacksmith'?'#ad7654':'#83c7b4';X.fillRect(x+10,n.y+15,24,25);X.fillStyle='#162126';X.fillRect(x+7,n.y+40,30,42);X.fillStyle='#d8b08a';X.fillRect(x+15,n.y+5,16,14);if(Math.abs(P.x-n.x)<110){X.fillStyle='#eaffff';X.font='700 12px monospace';X.textAlign='center';X.fillText('E · TƯƠNG TÁC',x+22,n.y-8)}X.restore()}
function drawTraps(){for(const t of room.traps){const x=t.x-cam;if(x<-80||x>W+80)continue;if(t.type==='spike'){X.fillStyle='#7e8c8e';for(let i=-36;i<=36;i+=18){X.beginPath();X.moveTo(x+i,GROUND);X.lineTo(x+i+9,GROUND-28);X.lineTo(x+i+18,GROUND);X.fill()}}if(t.type==='dart'){X.fillStyle='#4d5b5d';X.fillRect(x-10,GROUND-140,20,140);X.fillStyle='#a9c5c6';X.fillRect(x-15,GROUND-115,8,8)}if(t.type==='rock'&&t.triggered&&t.rockY<GROUND+40){X.fillStyle='#5e625b';X.fillRect(x-18,t.rockY,36,36);X.fillStyle='#85897e';X.fillRect(x-12,t.rockY+5,9,8)}if(t.type==='saw'){const sy=GROUND-55-Math.sin(t.t*.004+t.phase)*50;X.fillStyle='#b9c4c4';X.beginPath();X.arc(x,sy,22,0,Math.PI*2);X.fill();X.fillStyle='#4d5b5d';X.beginPath();X.arc(x,sy,8,0,Math.PI*2);X.fill()}}}
function drawEnemy(e){const cx=e.x-cam+e.w/2,top=e.y;if(cx<-150||cx>W+150)return;if(e.type==='slime'||e.type==='slimeking'){sprite(ASSETS.characters,'Slime',cx,top+(e.type==='slimeking'?-20:15),e.type==='slimeking'?105:55,e.dir<0);if(e.type==='slimeking'){X.fillStyle='#f3c95a';X.fillRect(cx-24,top-27,48,9)}}else if(e.type==='goblin'||e.type==='gobchief'){sprite(ASSETS.characters,e.type==='gobchief'?'Chief':'Guard',cx,top,e.type==='gobchief'?110:78,e.dir<0)}else if(e.type==='dragon'||e.type==='dragonalpha')drawDragon(e,cx,top);else if(e.type==='golem'||e.type==='golemboss')drawGolem(e,cx,top);else if(e.type==='centipede'||e.type==='cavequeen'||e.type==='centiqueen')drawCentipede(e,cx,top);else if(e.type==='halfdragon')drawHalfDragon(e,cx,top);else if(e.type==='corrupt')drawCorrupt(e,cx,top);else if(e.type==='dracula')drawDracula(e,cx,top);else if(e.type==='oni')drawOni(e,cx,top);else if(e.type==='treant')drawTreant(e,cx,top);else if(e.type==='ghoul')drawGhoul(e,cx,top);else sprite(ASSETS.characters,'King',cx,top,96,e.dir<0);X.fillStyle='#081015';X.fillRect(cx-e.w/2,top-10,e.w,5);X.fillStyle=e.type==='dracula'?'#d9172a':e.type==='treant'?'#8dbf4b':'#7ee59a';X.fillRect(cx-e.w/2,top-10,e.w*clamp(e.hp/e.max,0,1),5)}
function elementColor(el){return{ice:'#74dfff',fire:'#ff6b42',leaf:'#79d65b',earth:'#b28a5a',wind:'#b6fff3',dark:'#9d6dff',light:'#fff3a0',space:'#d66cff',electric:'#ffe45c'}[el]||'#99c7c7'}
function drawDragon(e,cx,top){const c=elementColor(e.element);X.save();X.translate(cx,top+45);if(e.dir<0)X.scale(-1,1);X.fillStyle='#16222b';X.beginPath();X.moveTo(-40,5);X.lineTo(-78,-38);X.lineTo(-32,-18);X.lineTo(-5,-5);X.lineTo(30,-26);X.lineTo(54,-8);X.lineTo(40,15);X.lineTo(4,26);X.closePath();X.fill();X.fillStyle=c;X.fillRect(-20,-2,48,22);X.fillRect(26,-9,28,18);X.fillRect(47,-13,12,8);X.fillRect(-48,12,30,9);X.fillStyle='#eefcff';X.fillRect(47,-3,4,3);X.restore()}
function drawGolem(e,cx,top){const c=elementColor(e.element);X.fillStyle='#5f625b';X.fillRect(cx-34,top+12,68,58);X.fillRect(cx-45,top+25,12,43);X.fillRect(cx+33,top+25,12,43);X.fillStyle='#7e8179';X.fillRect(cx-25,top,50,24);X.fillStyle=c;X.fillRect(cx-8,top+34,16,16);X.fillRect(cx-18,top+8,7,4);X.fillRect(cx+11,top+8,7,4)}
function drawCentipede(e,cx,top){const seg=e.type==='centipede'?6:10;for(let i=0;i<seg;i++){const x=cx+(i-seg/2)*20;X.fillStyle=i=