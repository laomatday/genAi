th.floor(R()*2);i++)arr.push(R()<.55?'golem':'dragon');return arr}if(f>=50){arr.push('dragon');for(let i=0;i<1+Math.floor(R()*2);i++)arr.push(R()<.5?'dragon':R()<.65?'centipede':'goblin');return arr}if(f>=20){for(let i=0;i<2+Math.floor(R()*3);i++)arr.push(R()<.45?'centipede':R()<.7?'goblin':'slime');return arr}for(let i=0;i<2+Math.floor(R()*3);i++)arr.push(R()<.55?'slime':'goblin');return arr}
function randomElement(f,type){const a=type==='dragon'?['ice','fire','leaf','earth','wind']:['ice','earth','fire','leaf','dark','light','space'];return a[Math.floor(rng(f*31415)()*a.length)]}
function buildFloor(f){save.floor=clamp(f,1,100);persist();const spec=floorSpec(save.floor),R=rng(save.floor*9187);room={spec,triggered:false,cleared:spec.kind==='quiet'||spec.kind==='trap'||spec.kind==='npc'||spec.kind==='shop',gateX:2670,traps:[],npc:null,platforms:[],oniLeft:0};enemies=[];projectiles=[];drops=[];fx=[];cam=0;P.x=90;P.y=GROUND-P.h;P.vy=0;P.jumps=0;P.inv=700;
 for(let i=0;i<5;i++)room.platforms.push({x:420+i*480+R()*180,y:310-R()*110,w:150+R()*100,h:18});
 if(spec.kind==='trap'||spec.kind==='mixed'){const types=['spike','dart','rock','saw'];for(let i=0;i<4;i++)room.traps.push({type:types[Math.floor(R()*types.length)],x:520+i*520+R()*180,y:GROUND,t:0,triggered:false,phase:R()*6.28})}
 if(spec.kind==='npc')room.npc={kind:['healer','blacksmith','scribe'][Math.floor(R()*3)],x:1350,y:GROUND-86,w:44,h:86};
 if(spec.kind==='shop')room.npc={kind:'shop',x:1350,y:GROUND-92,w:48,h:92};
 if(spec.kind==='gauntlet'){room.cleared=false;room.oniLeft=30}
 if(spec.kind==='boss'||spec.kind==='special'||spec.kind==='combat'||spec.kind==='mixed'||spec.kind==='gauntlet')room.cleared=false;
 updateHud();showFloorTitle(spec.name)}
function showFloorTitle(name){fx.push({type:'banner',text:'TẦNG '+save.floor+' · '+name,life:1500})}
function activateEncounter(){if(room.triggered)return;room.triggered=true;const f=save.floor,s=room.spec.special;if(room.spec.kind==='gauntlet'){spawnOniWave();return}if(s==='halfdragon'){enemies=[makeEnemy('halfdragon',1500,'all')];return}if(s==='corrupt'){enemies=[makeEnemy('corrupt',1500,['electric','fire','ice','earth'][f%4])];return}if(s==='dracula'){enemies=[makeEnemy('dracula',1550,'dark')];return}if(s==='treant'){enemies=[makeEnemy('treant',1580,'leaf')];return}if(room.spec.kind==='boss'){const t={5:'slimeking',15:'gobchief',25:'cavequeen',35:'abyss',45:'centiqueen',55:'dragonalpha',65:'golemboss'}[f]||'abyss';enemies=[makeEnemy(t,1550,t.includes('dragon')?'fire':'')];return}const pack=choosePack(f);enemies=pack.map((t,i)=>makeEnemy(t,1100+i*330,t==='dragon'||t==='golem'?randomElement(f+i,t):''))}
function spawnOniWave(){const n=Math.min(5,room.oniLeft);room.oniLeft-=n;for(let i=0;i<n;i++)enemies.push(makeEnemy('oni',1050+i*270,'fire'));fx.push({type:'banner',text:'ONI '+(30-room.oniLeft)+'/30',life:1000})}
function physDamage(v){const a=armor();return v*(1-(a?.phys||0))}function magicDamage(v){const a=armor();return v*(1-(a?.magic||0))}
function hurtPlayer(v,kind='phys'){if(P.inv>0)return;P.hp-=kind==='magic'?magicDamage(v):physDamage(v);P.inv=620;fx.push({type:'flash',life:150});if(P.hp<=0){P.hp=P.max;save.floor=Math.max(1,save.floor-1);save.coins=Math.max(0,save.coins-Math.floor(save.coins*.1));persist();buildFloor(save.floor);toast('Bạn gục ngã — mất 10% coin')}}
function damageEnemy(e,v,element='physical',knock=0){if(e.dead)return;let mult=1;if(e.type==='dracula'&&element==='light')mult=3;e.hp-=v*mult;e.vx+=knock;fx.push({type:'num',x:e.x+e.w/2,y:e.y-8,text:Math.round(v*mult),life:500,color:element==='light'?'#fff4a8':'#eaffff'});if(e.hp<=0)killEnemy(e)}
function killEnemy(e){e.dead=true;dropCoins(e.x+e.w/2,e.y+e.h,e.coin);if(['slimeking','gobchief','cavequeen','abyss','centiqueen','dragonalpha','golemboss','dracula','treant','halfdragon','corrupt'].includes(e.type)){save.keys++;toast('+1 Summon Key')}save.xp+=e.type==='slime'?10:e.type==='goblin'?25:80;while(save.xp>=100+save.level*45){save.xp-=100+save.level*45;save.level++;P.atk+=4;recalcHP(true);toast('LEVEL UP · '+save.level)}persist()}
function dropCoins(x,y,value){const n=Math.min(6,Math.max(1,Math.ceil(value/1000))),each=Math.floor(value/n);let remain=value;for(let i=0;i<n;i++){const v=i===n-1?remain:each;remain-=v;drops.push({x:x+(i-n/2)*9,y:y-12-Math.random()*12,vx:(Math.random()-.5)*2.5,vy:-2-Math.random()*2,value:v,age:0})}}
function attack(){if(P.attackCd>0)return;P.attackCd=230;const ranged=['Archer','Mage','Ifri','Sylph','Angel'].includes(HERO.cls);if(ranged){projectiles.push({x:P.x+P.w/2,y:P.y+30,vx:P.dir*9,vy:0,r:7,dmg:P.atk,owner:'p',element:HERO.skill3,life:1400})}else{const a={x:P.dir>0?P.x+P.w:P.x-90,y:P.y+7,w:90,h:65};enemies.forEach(e=>{if(!e.dead&&hit(a,e))damageEnemy(e,P.atk,'physical',P.dir*5)});fx.push({type:'slash',x:P.x+P.w/2+P.dir*38,y:P.y+36,dir:P.dir,life:180})}}
function skill(n){if(P.skill[n-1]>0)return;if(n===1){P.skill[0]=2500;const a={x:P.dir>0?P.x+P.w:P.x-150,y:P.y-10,w:150,h:90};enemies.forEach(e=>{if(hit(a,e))damageEnemy(e,P.atk*2.2,'physical',P.dir*9)});fx.push({type:'ring',x:P.x+P.w/2,y:P.y+40,r:55,life:300})}if(n===2){P.skill[1]=4000;P.x=clamp(P.x+P.dir*190,0,WORLD-P.w);P.inv=450;fx.push({type:'dash',x:P.x,y:P.y,life:250})}if(n===3){P.skill[2]=8000;const el=HERO.skill3;enemies.forEach(e=>{if(Math.abs((e.x+e.w/2)-(P.x+P.w/2))<270)damageEnemy(e,P.atk*4.2,el,Math.sign(e.x-P.x)*10)});fx.push({type:'ultimate',x:P.x+P.w/2,y:P.y+40,r:210,life:550,color:el==='light'?'#fff4a8':'#77eaff'})}}
function jump(){if(P.jumps<2){P.vy=-HERO.jump*(P.jumps?.92:1);P.jumps++;fx.push({type:'dust',x:P.x+P.w/2,y:P.y+P.h,life:220})}}
function updatePlayer(dt){const s=dt/16.666;P.attackCd=Math.max(0,P.attackCd-dt);P.dashCd=Math.max(0,P.dashCd-dt);P.inv=Math.max(0,P.inv-dt);P.skill=P.skill.map(v=>Math.max(0,v-dt));const m=(KEYS.d||KEYS.arrowright?1:0)-(KEYS.a||KEYS.arrowleft?1:0);P.vx=m*HERO.speed;if(m)P.dir=Math.sign(m);if(KEYS.k&&P.dashCd<=0){P.x+=P.dir*135;P.dashCd=900;P.inv=260;fx.push({type:'dash',x:P.x,y:P.y,