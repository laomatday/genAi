life:220})}P.vy+=.62*s;P.x=clamp(P.x+P.vx*s,0,WORLD-P.w);P.y+=P.vy*s;if(P.y+P.h>=GROUND){P.y=GROUND-P.h;P.vy=0;P.jumps=0}room.platforms.forEach(p=>{if(P.vy>=0&&P.x+P.w>p.x&&P.x<p.x+p.w&&P.y+P.h>=p.y&&P.y+P.h<=p.y+18){P.y=p.y-P.h;P.vy=0;P.jumps=0}});if(P.bleed>0){P.bleed-=dt;P.bleedTick-=dt;if(P.bleedTick<=0){P.bleedTick+=1000;P.hp-=50;fx.push({type:'num',x:P.x+P.w/2,y:P.y-4,text:'BLEED -50',life:600,color:'#ff5656'})}}if(!room.triggered&&!['quiet','trap','npc','shop'].includes(room.spec.kind)&&P.x>820)activateEncounter();if(!room.cleared&&P.x>room.gateX)P.x=room.gateX;if(P.x>WORLD-65&&room.cleared){if(save.floor>=100){toast('Đã tới tầng 100 · nội dung 76–100 sẽ mở rộng sau');P.x=WORLD-80}else buildFloor(save.floor+1)}if(P.x<0)P.x=0;cam+=((clamp(P.x-W*.38,0,WORLD-W))-cam)*Math.min(1,.12*s)}
function updateEnemies(dt){const s=dt/16.666;for(const e of enemies){if(e.dead)continue;e.cd-=dt;if(Math.abs(e.vx)>.05){e.x+=e.vx*s;e.vx*=.86}else{const dx=P.x-e.x,dir=Math.sign(dx)||1;e.dir=dir;const dist=Math.abs(dx);if(['slime','goblin','oni','ghoul','slimeking','gobchief','cavequeen','abyss','centiqueen'].includes(e.type)){e.x+=dir*(e.type==='slime'?.65:e.type==='oni'?1.25:.95)*s}else if(e.type==='centipede'){e.x+=dir*1.35*s}else if(['dragon','dragonalpha','halfdragon','corrupt','golem','golemboss','dracula','treant'].includes(e.type)){if(dist>260)e.x+=dir*.55*s;if(e.cd<=0){enemySkill(e);e.cd=e.type==='dracula'?1800:e.type==='treant'?1550:1300+Math.random()*900}}}if(hit(P,e)){hurtPlayer(e.dmg,e.type==='corrupt'||e.type==='dracula'||e.type==='golem'||e.type==='golemboss'?'magic':'phys');if(e.type==='dracula'&&P.inv>0){P.bleed=10000;P.bleedTick=1000}}}enemies=enemies.filter(e=>!e.dead);if(room.spec.kind==='gauntlet'&&room.triggered&&enemies.length===0&&room.oniLeft>0)spawnOniWave();if(room.triggered&&!room.cleared&&enemies.length===0&&room.oniLeft===0){room.cleared=true;P.hp=Math.min(P.max,P.hp+P.max*.08);fx.push({type:'banner',text:'KHU VỰC AN TOÀN · TIẾP TỤC →',life:1200})}}
function enemySkill(e){const dx=P.x-e.x,dir=Math.sign(dx)||1;if(e.type==='dracula'){if(Math.random()<.38){enemies.push(makeEnemy('ghoul',e.x+dir*120,'dark'));fx.push({type:'banner',text:'DRACULA TRIỆU HỒI NGẠ QUỶ',life:800})}else projectiles.push({x:e.x+e.w/2,y:e.y+25,vx:dir*5.2,vy:0,r:12,dmg:e.dmg*.55,owner:'e',element:'dark',life:2200,bleed:true})}else if(e.type==='treant'){for(let i=-2;i<=2;i++)projectiles.push({x:P.x+i*70,y:GROUND-10,vx:0,vy:-1,r:18,dmg:e.dmg*.38,owner:'e',element:'earth',life:700,root:true})}else if(e.type==='golem'||e.type==='golemboss'){projectiles.push({x:e.x+e.w/2,y:GROUND-18,vx:dir*4.5,vy:0,r:16,dmg:e.dmg*.55,owner:'e',element:e.element||'earth',life:2000})}else{projectiles.push({x:e.x+e.w/2,y:e.y+30,vx:dir*5.5,vy:-.3,r:10,dmg:e.dmg*.45,owner:'e',element:e.element||'fire',life:1900})}}
function updateProjectiles(dt){const s=dt/16.666;for(const b of projectiles){b.x+=b.vx*s;b.y+=b.vy*s;b.life-=dt;if(b.root){b.y-=.2*s}if(b.owner==='p'){for(const e of enemies)if(!e.dead&&Math.hypot(b.x-(e.x+e.w/2),b.y-(e.y+e.h/2))<b.r+e.w*.35){damageEnemy(e,b.dmg,b.element,b.vx*.4);b.life=0;break}}else if(P.inv<=0&&Math.hypot(b.x-(P.x+P.w/2),b.y-(P.y+P.h/2))<b.r+22){hurtPlayer(b.dmg,'magic');if(b.bleed){P.bleed=10000;P.bleedTick=1000}b.life=0}}projectiles=projectiles.filter(b=>b.life>0&&b.x>-100&&b.x<WORLD+100&&b.y>-100&&b.y<H+100)}
function updateDrops(dt){const s=dt/16.666;for(const d of drops){d.age+=dt;d.vy+=.18*s;d.x+=d.vx*s;d.y+=d.vy*s;if(d.y>GROUND-8){d.y=GROUND-8;d.vy*=-.35;d.vx*=.8}const dist=Math.hypot(d.x-(P.x+P.w/2),d.y-(P.y+P.h/2));if(d.age>350&&dist<260){d.vx+=(P.x+P.w/2-d.x)*.004*s;d.vy+=(P.y+P.h/2-d.y)*.004*s}if(dist<28){save.coins+=d.value;d.dead=true;persist()}}drops=drops.filter(d=>!d.dead)}
function trapDamage(){hurtPlayer(Math.max(70,P.max*.07),'phys')}
function updateTraps(dt){for(const t of room.traps){t.t+=dt;if(t.type==='rock'&&!t.triggered&&Math.abs(P.x-t.x)<90){t.triggered=true;t.rockY=40}if(t.type==='rock'&&t.triggered&&t.rockY<GROUND){t.rockY+=dt*.34;if(hit(P,{x:t.x-18,y:t.rockY,w:36,h:36})){trapDamage();t.rockY=GROUND+100}}if(t.type==='dart'&&t.t>1600&&Math.abs(P.x-t.x)<700){t.t=0;projectiles.push({x:t.x,y:GROUND-105,vx:-6,vy:0,r:6,dmg:P.max*.06,owner:'e',element:'physical',life:1800})}if(t.type==='spike'&&P.y+P.h>=GROUND-3&&P.x+P.w>t.x-38&&P.x<t.x+38)trapDamage();if(t.type==='saw'){const sy=GROUND-55-Math.sin(t.t*.004+t.phase)*50;if(hit(P,{x:t.x-20,y:sy-20,w:40,h:40}))trapDamage()}}}
function nearestNPC(){return room.npc&&Math.abs((P.x+P.w/2)-(room.npc.x+room.npc.w/2))<95?room.npc:null}
function interact(){const n=nearestNPC();if(!n){toast('Không có ai để tương tác');return}openNPC(n)}
function openNPC(n){const title={shop:'Thương Nhân Tầng '+save.floor,healer:'Tu Sĩ Lưu Lạc',blacksmith:'Thợ Rèn Mù',scribe:'Người Vẽ Bản Đồ'}[n.kind];$('npcTitle').textContent=title;const text=$('npcText'),acts=$('npcActions');acts.innerHTML='';if(n.kind==='shop'){text.innerHTML='Mỗi 10 tầng là một trạm shop an toàn. Dùng coin để hồi phục, mua Summon Key hoặc nâng trang bị.';addNpcAction('Hồi đầy HP · 500 coin',()=>buy(500,()=>P.hp=P.max));addNpcAction('Summon Key · 1500 coin',()=>buy(1500,()=>save.keys++));addNpcAction('Tăng ATK +5 · '+(1800+save.floor*40)+' coin',()=>buy(1800+save.floor*40,()=>{save.atkBonus+=5;P.atk+=5}))}if(n.kind==='healer'){text.innerHTML='"Hầm ngục không chỉ có quái. Đôi lúc một người sống sót còn quý hơn kho báu."';addNpcAction('Hồi 40% HP · 250 coin',()=>buy(250,()=>P.hp=Math.min(P.max,P.hp+P.max*.4)))}if(n.kind==='blacksmith'){text.innerHTML='"Tôi nghe được tiếng thép của cậu từ ba hành lang trước."';addNpcAction('Tăng ATK +3 · 800 coin',()=>buy(800,()=>{save.atkBonus+=3;P.atk+=3}))}if(n.kind==='scribe'){text.innerHTML='"Đi về phía phải. Không có portal đâu — chỉ có đá, bẫy, và thứ đang chờ sau bóng tối."<br><br>Tầng '+save.floor+': '+room.spec.name+'.';addNpcAction('Nhận chỉ dẫn',()=>toast('Mũi tên trên đường sẽ chỉ lối'))}$('npcModal').classList.add('show')}
function addNpcAction(