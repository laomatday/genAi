import {getPipeline} from "@/lib/data/crm";import {PipelineBoard} from "@/components/PipelineBoard";
export default async function PipelinePage(){const {stages,cards}=await getPipeline();return <><div className="page-head"><div><h1>Pipeline</h1><p>Kéo lead giữa các cột. Mỗi card là hành trình của lead tại một cơ sở.</p></div><button className="primary-btn">+ Thêm lead</button></div><div className="gem-filter"><button>Tất cả cơ sở⌄</button><button>Tất cả EC⌄</button><button>Chỉ lead nóng</button><button>Lead quá hạn</button></div><PipelineBoard initialStages={stages} initialCards={cards}/></>}
