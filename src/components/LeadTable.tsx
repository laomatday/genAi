import type {LeadCenterCard} from "@/lib/types";import {money} from "@/lib/format";
export function LeadTable({cards}:{cards:LeadCenterCard[]}){return <div className="table-shell"><table><thead><tr><th>Khách hàng</th><th>Trạng thái</th><th>Cơ sở</th><th>EC phụ trách</th><th>Việc tiếp theo</th><th>Giá trị</th></tr></thead><tbody>{cards.map(c=><tr key={c.id}><td><b>{c.guardian_name}</b><small>{c.phone} · {c.student_name}</small></td><td><span className={`badge ${c.temperature==="Nóng"?"red":c.temperature==="Ấm"?"amber":"grey"}`}>{c.stage_name}</span></td><td><div className="chip-row"><span className="center-chip primary">{c.center_name}</span>{c.related_centers.map(x=><span className="center-chip" key={x}>{x}</span>)}</div></td><td>{c.owner_name??"Chưa giao"}</td><td><b>{c.next_action??"Chưa có"}</b></td><td>{money(c.expected_value)}</td></tr>)}</tbody></table></div>}
